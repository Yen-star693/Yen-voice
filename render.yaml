services:
  - type: web
    name: yen-voice-bot
    env: python
    buildCommand: |
      pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: TOKEN
        sync: false
      - key: GROQ_KEY
        sync: false